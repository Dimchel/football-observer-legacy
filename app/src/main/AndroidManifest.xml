<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dimchel.footballobserver">

    <application
        android:name=".Application"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/core_ui_AppTheme">

        <!-- activities -->
        <activity android:name=".presentation.launch.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.dimchel.feature_competitions.presentation.CompetitionsActivity" />
<!--        <activity android:name=".presentation.league.LeagueActivity" />-->
<!--        <activity android:name=".presentation.club.ClubActivity" />-->

    </application>

</manifest>